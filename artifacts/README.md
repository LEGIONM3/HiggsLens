# HiggsLens Artifacts Directory

Stores model checkpoints, evaluation metrics, calibration reports, and generated model cards.

All generated binary artifacts and database files are excluded from Git via `.gitignore`.
